---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span
  class="inline-flex items-center px-3 py-1.5 rounded-full text-sm font-medium"
  style="background-color: var(--color-bg-primary); color: var(--color-brand);"
>
  {text}
</span>
